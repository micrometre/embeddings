{
  "name": "browser-embeddings",
  "version": "1.0.0",
  "type": "module",
  "description": "Browser-based sentence embeddings with FAISS vector search",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "faiss-node": "^0.5.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
